Open Video optoin does not work.
Requires VLC installed, vlc python API, easygui, tkinter

Following files must be in the same directory:
    all videos to be processed,
    main.py
    trimmer executable,
    inputs.txt,
    outputs.txt,
    _video_trimming_data.txt
